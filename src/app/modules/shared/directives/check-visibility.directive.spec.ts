import { CheckVisibilityDirective } from './check-visibility.directive';

describe('CheckVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
